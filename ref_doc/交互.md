用户与 ABC 系统交互数据流示例（基于双轨差异分析场景）
1. 场景概述
本示例展示一位科研人员通过 ABC 网页聊天界面，发起基因组差异表达分析请求，并在系统完成双轨（传统生信工具 vs 大模型）分析后，查看对比结果并提交反馈的完整过程。整个过程以用户与系统的对话为主线，突出数据流动和用户可见的交互内容。
【大模型未必能分析这种生物学数据】
2. 交互数据流详细步骤
步骤1：用户发起分析请求
用户输入（在聊天对话框发送）：
希望使用3月6日的数据，做差异表达分析，并分别用传统工具和大模型分析，对比结果。
【数据来源是什么？】
系统返回（流式显示，逐条推送）：
[系统] 正在解析您的请求...
[系统] 识别到意图：差异表达分析；时间条件：2026-03-06；双轨对比要求：是。
[系统] 正在查询本体库，寻找符合条件的数据集...
[系统] 找到数据集「原始表达矩阵」（ID: expr_matrix_raw），包含10个基因、6个样本（对照组3个，处理组3个）。
[系统] 准备启动双轨分析：
•工具轨：调用 DESeq2 进行统计检验 【单步】
•大模型轨：调用 DeepSeek 进行基于数据摘要的推理
步骤2：系统执行分析并实时更新进度
系统推送（分析过程中）：
[系统] 工具轨：数据预处理完成，开始差异检验...
[系统] 大模型轨：正在提取表达摘要，构建提示词...
[系统] 大模型轨：已提交推理请求，等待响应...
[系统] 工具轨：DESeq2 分析完成，生成差异基因列表。
[系统] 大模型轨：收到推理结果，解析完成。
【这里其实都是前端的信息】
步骤3：展示双轨对比结果
系统推送（分析完成，展示对比视图）：
[系统] 双轨分析已完成！以下是结果对比：
工具轨（DESeq2）结果
•显著差异基因：2个
￮Gene3：上调，log2FC = 1.6，p = 0.0001
￮Gene7：下调，log2FC = -2.0，p = 0.00005
[图片下载失败]
大模型轨（DeepSeek）结果
•显著差异基因：1个
￮Gene3：上调（理由：处理组均值248 vs 对照组81，差异显著）
•注：未检测到下调基因
•推理摘要：基于表达均值比较，Gene3显著上调，其余基因无明显变化。
一致性对比
•共同检出的基因：Gene3
•不一致的基因：Gene7（工具轨检出下调，大模型轨未检出）
•重合率：1/2
您可以在下方对每个结果进行评价，也可直接回复文字反馈。
同时，系统在聊天界面下方嵌入一个对比卡片，包含两个结果区域的点赞/点踩按钮和评论输入框。
步骤4：用户对结果进行评价
用户操作：点击大模型轨结果区域的“点踩”按钮，并附加评论。
用户评论：
漏掉了Gene7，这个基因很重要。
系统返回：
[系统] 感谢您的反馈！已记录对大模型轨的评价（负面）。系统将据此优化大模型提示词，并在基因Gene7上添加注意标签。
步骤5：系统更新本体（用户无感知，但可通过后续查询验证）
系统内部：将用户反馈转化为结构化数据，更新Neo4j中相关节点属性（如降低大模型流程置信度、为Gene7添加警告标记）。
【这里更新本体更新的是什么？Gene7？】
步骤6：用户后续查询（可选）
用户输入：
展示基因Gene7的详细信息。
系统返回：
[系统] 基因 Gene7（染色体：chr3）
•在本次分析中被工具轨判定为下调（log2FC = -2.0），但大模型轨未能检出。
•有用户反馈指出此基因曾被漏检，建议在分析涉及下调基因时关注大模型的表现。
•表达数据：
￮对照组：421, 418, 423
￮处理组：102, 98, 105
3. 关键数据格式说明
3.1 用户输入
纯文本，支持自然语言形式的分析请求表述。
3.2 系统输出
采用流式文本形式，逐步向用户展示请求处理、分析执行的实时状态；最终分析结果会结合Markdown格式（用于呈现列表、标题、数据表格等）和交互组件（如点赞/点踩按钮、评论输入框）；结果中的图片通过URL嵌入，由前端直接加载显示。
3.3 对比卡片数据结构（示例）
JSON
{
  "type": "comparison",
  "title": "双轨分析结果对比",
  "columns": [
    {
      "name": "工具轨 (DESeq2)",
      "content": {
        "genes": ["Gene3 (up)", "Gene7 (down)"],
        "plot_url": "http://abc-breeding-claw.ai/plots/volcano_tool.png"
      }
    },
    {
      "name": "大模型轨 (DeepSeek)",
      "content": {
        "genes": ["Gene3 (up)"],
        "summary": "处理组均值248 vs 对照组81，差异显著"
      }
    }
  ],
  "consistency": {
    "overlap": ["Gene3"],
    "tool_only": ["Gene7"],
    "llm_only": []
  }
}
4. 总结
本数据流完整展示了用户从输入自然语言分析请求，到系统执行双轨分析、展示对比结果，再到用户提交反馈、系统完成知识沉淀的全过程。系统通过网页聊天界面与用户保持透明化交互，逐步呈现请求解析、分析执行的推理过程，并以可视化的对比视图辅助用户科研决策，最后将用户的反馈信息纳入系统本体更新，实现知识的持续沉淀。开发人员可依据此流程设计前端聊天交互组件和后端响应数据格式，确保用户与系统的每一次交互都有清晰、可追溯的数据流动。
|（注：文档部分内容可能由 AI 生成)